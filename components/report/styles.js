import { StyleSheet } from "react-native";
export const reportStyles = StyleSheet.create({
  reportContainer: {
    fontSize: 12,
    flex: 10
  },
  infomsg:{
      fontSize:18,
      fontWeight:"bold"
  }
});
